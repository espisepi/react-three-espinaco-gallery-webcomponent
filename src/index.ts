import './styles.css' // OPCIONAL
import {createGalleryWebcomponent} from './webcomponents/gallery';

// Poner aqui los web components que quiera hacerle build
// Comentar todos los web components excepto el que vaya a hacerle build

// Web Components ================

createGalleryWebcomponent();